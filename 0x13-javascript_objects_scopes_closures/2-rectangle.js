#!/usr/bin/node
module.exports = class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;

    if (self.width <= 0 || self.height <= 0 ) {
      const Rectangle = {};
    }
  }
}